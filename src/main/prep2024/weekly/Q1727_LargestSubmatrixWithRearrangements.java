package main.prep2024.weekly;

public class Q1727_LargestSubmatrixWithRearrangements {
  public int largestSubmatrix(int[][] matrix) {
    // Use BFS to get the distances of all nuts relative to the squirrel

    // Use BFS to get the distances of all nuts relative to the tree

    // Locate the first nut. Result = D(S, N1) + D(T, N1) for the smallest possibility.

    // Calculate the back and forth for all other nuts. Result = 2*D(T, N2) + 2*D(T,N3) ...
    return 0;
  }
}
